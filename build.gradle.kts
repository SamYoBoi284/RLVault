// Top-level build file. No dependencies declared here directly —
// versions are centralized via the plugins block + libs used in app/build.gradle.kts.
plugins {
    id("com.android.application") version "8.5.2" apply false
    id("org.jetbrains.kotlin.android") version "1.9.24" apply false
}
